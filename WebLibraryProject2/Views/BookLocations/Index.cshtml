@using WebLibraryProject2.Controllers
@model IEnumerable<WebLibraryProject2.Models.BookLocation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if (User.Identity.isAdmin())
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Publication.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reader.First)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Room)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Place)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsTaken)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Publication.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reader.First)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Room)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Place)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsTaken)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            {
                <p>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </p>
            }
        </td>
    </tr>
}

</table>
